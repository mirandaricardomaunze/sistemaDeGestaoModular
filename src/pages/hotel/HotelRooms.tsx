/**
 * Hotel Rooms (Quartos)
 * 
 * Main rooms management page - reuses existing Hospitality component
 * but will be the default landing page for hotel module
 */

import Hospitality from '../Hospitality';

export default function HotelRooms() {
    return <Hospitality />;
}

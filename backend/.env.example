# ================================
# Sistema Backend Environment Config
# ================================

# Server
PORT=3001
NODE_ENV=development

# PostgreSQL Database
DATABASE_URL="postgresql://postgres:1993@localhost:5432/sistema_db?schema=public"

# JWT Configuration (IMPORTANTE: USE UMA CHAVE FORTE E SEGURA!)
# Gere uma com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="SUBSTITUA_POR_UMA_CHAVE_SECRETA_FORTE_MINIMO_32_CARACTERES"
JWT_EXPIRES_IN="7d"

# CORS (separar múltiplos domínios por vírgula)
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"
FRONTEND_URL="http://localhost:5173"

# Redis Configuration (para rate limiting e cache)
REDIS_URL="redis://localhost:6379"
REDIS_PASSWORD=""

# Company Defaults
DEFAULT_IVA_RATE=16
DEFAULT_INSS_EMPLOYEE_RATE=3
DEFAULT_INSS_EMPLOYER_RATE=4

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_SCHEDULE="0 2 * * *"  # Cron: 2h da manhã todo dia
BACKUP_RETENTION_DAYS=30     # Manter backups por 30 dias

# Google Drive Integration (Optional)
GDRIVE_ENABLED=false
GDRIVE_CLIENT_ID="1029794681906-tedr7um3b8644at4ju8eh5nc6cktvuid.apps.googleusercontent.com"
GDRIVE_CLIENT_SECRET="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"
GDRIVE_REDIRECT_URI="http://localhost:3001/api/gdrive/callback"
GDRIVE_REFRESH_TOKEN=""      # Obter após autorização
GDRIVE_FOLDER_ID=""          # ID da pasta no Drive (opcional)

# Monitoring & Logging
LOG_LEVEL="info"
SENTRY_DSN=""  # Opcional: para error tracking

# Email Configuration (opcional)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASSWORD=""

# n8n AI Integration
N8N_WEBHOOK_URL="http://localhost:5678/webhook/chat-ai"
N8N_TIMEOUT=30000

# Google Gemini (configurar no n8n, não aqui)
GEMINI_API_KEY="AIzaSyBvCy0dcoykImostqSrKIULgXFnqc5AZGA"
